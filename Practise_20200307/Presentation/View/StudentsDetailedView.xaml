<UserControl x:Class="Presentation.View.StudentsDetailedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Expander Header="{Binding}">
            <StackPanel Grid.Column="1" 
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Top">                
                <Label Content="DateOfBirth"/>
                <TextBox Text="{Binding Path=DateOfBirth}"/>
                <Label Content="EnteranceDate"/>
                <TextBox Text="{Binding Path=EnteranceDate}"/>
                <Label Content="Email"/>
                <TextBox Text="{Binding Path=Email}"/>
                <Label Content="PhoneNumber"/>
                <TextBox Text="{Binding Path=PhoneNumber}"/>
            </StackPanel>
        </Expander>  
    </Grid>
</UserControl>
