<UserControl x:Class="View.AdminView.OrderItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.AdminView"
             xmlns:templates="clr-namespace:View.AdminView.Templates"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <UserControl.Resources>
        <DataTemplate x:Key="orderItemsTemplate">
            <templates:OrderItemsTemplate/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.25*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
            <Label Margin="5"
                   Grid.Row="0"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Content="Order Items"/>
            <ListView ItemsSource="{Binding Path=OrderItems}"
                      ItemTemplate="{StaticResource ResourceKey=orderItemsTemplate}"
                      Margin="5"
                      Grid.Row="1"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"/>
    </Grid>
</UserControl>
